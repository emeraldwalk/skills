# CLAUDE.md

## Editing skills

All edits must be made in the top-level skill folders in this repo (e.g. `pixel-editing/`).

**Never edit `~/.claude/skills/`.** In a dev container it is a read-only bind mount; locally it is a symlink back to this repo. Either way, it is not the editable source.
